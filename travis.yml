language: ruby

gemfile: drupal/sites/all/themes/checkdesk/Gemfile

bundler_args: --without development

env:
  - CI=true

rvm:
  - 1.9.3

branches:
  only:
    - develop

notifications:
  email:
    recipients:
      - cblow@meedan.net